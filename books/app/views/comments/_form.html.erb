<%= form_for([@book.author, @book, @comment]) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="field" type="hidden">
    <%#= f.label :book_id %><br>
    <%= f.text_field :book_id, :type => 'hidden' %>
  </div>
  <div class="field" type="hidden">
    <%#= f.label :user_id, :type => 'hidden' %><br>
    <%= f.text_field :user_id, :type => 'hidden' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
