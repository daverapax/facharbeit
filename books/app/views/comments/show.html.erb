<p id="notice"><%= notice %></p>

<p>
  <strong>Comment:</strong>
  <%= @comment.comment %>
  <%= @comment.like %>
</p>

<p>
  <strong>Book:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>User:</strong>
  <%= @user.name %>
</p>

<%= link_to 'like', author_book_comment_path(@book.author.id, @book.id, @comment.id, :like => 'like') %>
<%#= link_to 'Edit', edit_comment_path(@comment.id) %>
<%#= link_to 'Back', comments_path %>
