<p id="notice"><%= notice %></p>

<h1>Liste der Bücher von <%= @authors.name %> </h1>

<table>
  <thead>
    <tr>
      <th>Titel</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= link_to 'Show', author_book_path(@authors.id, book.id) %></td>
        <td><%#= link_to 'Edit', edit_book_path(book) %></td>
        <td><%#= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_author_book_path, :class => 'button' %>
<%= link_to 'Back', author_path(params[:author_id]), :class => 'button' %>
